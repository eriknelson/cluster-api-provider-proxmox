# Changelog

## [Unreleased]

Changes to this fork are released from the protected `main` branch. Upstream
CAPMOX tags provide the version baseline; fork releases tick the `-nsk.N`
patch-set suffix. The matching image tag omits the leading `v`.
